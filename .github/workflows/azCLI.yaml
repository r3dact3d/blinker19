name: Azure CLI
on:
  push:
    branches:
      - 'main'
    paths:
      - 'trigger.md'
  workflow_dispatch:

jobs:
  Setup:
    name: Setup Ubuntu runner
    runs-on: ubuntu-latest
    steps:
#    - name: Azure login
#        uses: azure/login@v1
#        with:
#          creds: ${{ secrets.AZURE_CREDENTIALS }}
                            
      - name: Spin up CLI container
        uses: azure/CLI@v1
        with:
          azcliversion: latest
          inlineScript: |
            #az account set --subscription ${{ secrets.SUBSCRIPTION_ID }}
            wget -O az-aroext-latest.whl "https://aka.ms/az-aroext-latest.whl"
            az extension add -s az-aroext-latest.whl
            ls *
            az extension list
            az aro create cluster --help
